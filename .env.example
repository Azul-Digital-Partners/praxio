# Praxio Environment Variables
# Copy to .env and fill in real values. Never commit .env.

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/praxio

# Server
PORT=3000
NODE_ENV=development

# Agent runtime (optional — leave blank to use subscription mode)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
